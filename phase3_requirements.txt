# GeoAuPredict Phase 3: Predictive Modeling Requirements
# Requirements for machine learning models and geospatial analysis

# Core data science libraries
numpy>=1.21.0
pandas>=1.5.0
scikit-learn>=1.1.0  # For traditional ML models and preprocessing

# Machine learning libraries
xgboost>=1.7.0  # Gradient boosting
lightgbm>=3.3.0  # Light gradient boosting


# Deep Learning (optional - for CNN/LSTM models)
# torch>=1.13.0  # PyTorch for deep learning models
# torchvision>=0.14.0  # Computer vision utilities
# torch-geometric>=2.3.0  # Graph Neural Networks

# Geospatial libraries (from Phase 2)
geopandas>=0.12.0
shapely>=2.0.0
rasterio>=1.3.0

# Scientific computing
scipy>=1.9.0  # For spatial statistics and interpolation

# Visualization
matplotlib>=3.6.0
seaborn>=0.12.0
plotly>=5.15.0  # For interactive plots

# Model interpretation and evaluation
shap>=0.41.0  # SHAP values for model interpretation
lime>=0.2.0  # LIME for local explanations

# Hyperparameter optimization
optuna>=3.2.0  # Advanced hyperparameter tuning
# OR
# hyperopt>=0.2.7  # Alternative hyperparameter optimization

# Spatial statistics
# pysal>=2.7.0  # PySAL for spatial econometrics
# OR
# geopy>=2.3.0  # For geographic calculations

# Configuration and utilities
pyyaml>=6.0  # For configuration files
tqdm>=4.64.0  # For progress bars
joblib>=1.2.0  # For parallel processing and model persistence

# Jupyter notebook support (for development)
jupyter>=1.0.0
ipykernel>=6.17.0
ipywidgets>=8.0.0

# Model serialization and deployment
#pickle5>=0.0.11  # For model serialization
# OR for newer Python versions
# pickle  # Built-in for model serialization

# Optional: Cloud storage and API integrations
# boto3>=1.26.0  # AWS S3 integration
# requests>=2.28.0  # HTTP requests for APIs

# Development and testing
pytest>=7.2.0
pytest-cov>=4.0.0
black>=22.10.0  # Code formatting
flake8>=5.0.0  # Code linting

# Installation notes:
# 1. Core ML requirements: pip install -r requirements.txt
# 2. For full ML capabilities: pip install xgboost lightgbm scikit-learn
# 3. For deep learning (optional): pip install torch torchvision
# 4. For hyperparameter tuning: pip install optuna
# 5. For model interpretation: pip install shap lime

# Version compatibility notes:
# - All packages tested with Python 3.8-3.11
# - XGBoost and LightGBM may require specific compiler versions
# - PyTorch installation may vary by platform (CPU/GPU)

# Development installation (includes all optional dependencies):
# pip install -e ".[dev]"

# Production installation (minimal dependencies):
# pip install -e ".[prod]"

# GPU support (optional):
# For CUDA-enabled deep learning, install PyTorch with CUDA support:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118

# Note on spatial libraries:
# Some geospatial packages may require system-level dependencies:
# - GDAL (for rasterio)
# - GEOS (for shapely)
# - PROJ (for coordinate transformations)
