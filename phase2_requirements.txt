# GeoAuPredict Phase 2: Geospatial Feature Engineering Requirements
# Requirements for spectral indices calculation, terrain analysis, and geological processing

# Core geospatial libraries
numpy>=1.21.0
pandas>=1.5.0
geopandas>=0.12.0
shapely>=2.0.0
rasterio>=1.3.0

# Terrain analysis libraries
richdem>=0.3.4  # For advanced terrain analysis (slope, curvature, etc.)
# OR
# gdal>=3.5.0  # Alternative terrain analysis

# Scientific computing
scipy>=1.9.0  # For spatial distance calculations
scikit-learn>=1.1.0  # For data preprocessing and validation

# Visualization (optional, for development)
matplotlib>=3.6.0
seaborn>=0.12.0
folium>=0.14.0  # For interactive web maps

# Parallel processing (optional, for large datasets)
joblib>=1.2.0
dask>=2022.10.0  # For chunked raster processing

# Configuration and utilities
pyyaml>=6.0  # For configuration files
tqdm>=4.64.0  # For progress bars
python-dotenv>=0.21.0  # For environment variables

# Development and testing
pytest>=7.2.0
pytest-cov>=4.0.0
black>=22.10.0  # Code formatting
flake8>=5.0.0  # Code linting

# Jupyter notebook support (for development)
jupyter>=1.0.0
ipykernel>=6.17.0
ipywidgets>=8.0.0

# Optional: Cloud storage and API integrations
# boto3>=1.26.0  # AWS S3 integration
# requests>=2.28.0  # HTTP requests for APIs

# Optional: Machine learning integration (for model training)
# torch>=1.13.0  # PyTorch for deep learning
# torchvision>=0.14.0  # Computer vision utilities

# Installation notes:
# 1. Install core requirements: pip install -r requirements.txt
# 2. For terrain analysis, install either:
#    - pip install richdem  # Recommended for accuracy
#    - conda install gdal    # Alternative option
# 3. For full functionality: pip install -r requirements-full.txt

# Version compatibility notes:
# - All packages tested with Python 3.8-3.11
# - GDAL may require system-level installation on some platforms
# - richdem requires C++ compiler for installation on Windows

# Development installation (includes all optional dependencies):
# pip install -e ".[dev]"

# Production installation (minimal dependencies):
# pip install -e ".[prod]"
